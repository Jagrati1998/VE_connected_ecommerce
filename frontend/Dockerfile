# Step 1: Use the official Node.js 16 image as the base image
FROM node:18.12.1-alpine

# Step 2: Set the working directory in the container
WORKDIR /app

# Step 3: Copy the package.json and package-lock.json to the container
COPY package*.json ./

# Step 4: Install dependencies
RUN npm install

# Step 5: Copy the rest of the app's source code to the container
COPY . .

# Step 6: Build the React app for production
RUN npm run build

# Step 7: Serve the app with a simple server (e.g., serve package)
RUN npm install -g serve

# Step 8: Expose the port the app will run on
EXPOSE 3000

# Step 9: Command to run the app
CMD ["serve", "-s", "build"]
